# Frontend (Vite) — http://localhost:8080
VITE_APP_URL=http://localhost:8080
VITE_PORT=8080

# Leave empty so the Vite dev server proxies /api to the backend.
# Direct http://localhost:4000 calls often fail on Windows (IPv6 localhost).
VITE_API_URL=
